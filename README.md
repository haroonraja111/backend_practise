# Django CI/CD Project

A production-ready Django project with CI/CD using GitHub Actions.

## Quick Start

1. Clone and setup:
   ```bash
   git clone <your-repo>
   cd django-cicd-project/backend
   cp .env.example .env